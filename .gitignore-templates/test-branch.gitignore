# =============================================================================
# .gitignore for TEST branch (Testing & QA Validation)
# Purpose: INCLUDE test artifacts, coverage reports, and validation data
# =============================================================================

# Dependencies (Always exclude)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build Outputs (Exclude - rebuilt for testing)
build/
dist/
out/
.next/
*.tsbuildinfo

# Environment & Secrets (Critical exclusion)
.env*
secrets/
!secrets/.gitkeep
.secrets/
docker-secrets/
*.pem
*.key
*.csr
certs/

# Testing & QA Data (INCLUDE for analysis)
# test-results/ (KEEP for test analysis)
# coverage/ (KEEP for coverage analysis)
# junit.xml (KEEP for CI analysis)
# playwright-report/ (KEEP for debugging)
# visual-regression-diff/ (KEEP for validation)
# screenshot-diff/ (KEEP for visual testing)
# storybook-static/ (KEEP for component testing)

# Only exclude temporary testing data
.stryker-tmp/
.nyc_output

# Development & Coordination Tools (INCLUDE for test coordination)
# .claude/ (KEEP for QA settings and test commands)
# .roo/ (KEEP for test rules and validation modes)
# CLAUDE.md (KEEP for test context and instructions)
# memory/ (KEEP for test run data and coordination)
# .roomodes (KEEP for test mode configurations)
# .mcp.json (KEEP for test MCP configurations)
# .swarm/ (KEEP for test swarm coordination)
# .hive-mind/ (KEEP for collective intelligence analysis)

# Exclude only personal/local settings
.claude/settings.local.json
.claude/personal/
.mcp.json.local
claude-flow.config.local.json

# IDE Settings (Include for test consistency)
.vscode/
.idea/
*.swp
*.swo
*~

# OS Files
.DS_Store
Thumbs.db
.Trashes

# Runtime Data (Exclude volatile)
logs/
postgres_data/
redis_data/
*.db
*.db-journal
*.db-wal
*.sqlite
*.sqlite-journal
*.sqlite-wal

# Media & Uploads
uploads/
youtube/
downloads/

# Cache & Temp
.cache/
.parcel-cache/
tmp/
temp/

# Docker Runtime
docker-compose.override.yml

# Backups
backups/
*.backup
*.bak

# Exclude executables but keep test scripts
claude-flow
claude-flow.bat
claude-flow.ps1
hive-mind-prompt-*.txt
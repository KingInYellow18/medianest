# =============================================================================
# .gitignore for MAIN BRANCH (Production-Ready)
# Purpose: Clean production deployments with minimal essential files only
# =============================================================================

# Dependencies (Always exclude - rebuilt by CI/CD)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm/

# Build Outputs (Exclude - CI/CD rebuilds)
build/
dist/
out/
.next/
coverage/
.nyc_output
*.tsbuildinfo

# Compiled shared files (exclude - built by pipeline)
shared/**/*.js
shared/**/*.js.map
shared/**/*.d.ts
shared/**/*.d.ts.map
!shared/tsconfig.json

# Environment & Secrets (CRITICAL - never commit)
.env*
!.env.example
secrets/
!secrets/.gitkeep
.secrets/
docker-secrets/
*.pem
*.key
*.crt
*.csr
certs/

# Development Tools (Exclude from production)
.vscode/launch.json
.vscode/extensions.json
.idea/workspace.xml
.idea/tasks.xml
*.swp
*.swo
*~

# Testing & QA (Exclude - not needed in production)
test-results/
junit.xml
results.json
visual-regression-diff/
screenshot-diff/
playwright-report/
storybook-static/
.stryker-tmp/

# OS Files
.DS_Store
Thumbs.db
.Trashes
*.log

# Runtime Data (Exclude - environment-specific)
logs/
postgres_data/
redis_data/
*.db
*.sqlite
*.db-journal
*.db-wal
*.sqlite-journal
*.sqlite-wal

# Media & User Uploads (Exclude - separate storage)
uploads/
youtube/
downloads/

# Cache & Temporary Files
.cache/
.parcel-cache/
tmp/
temp/

# Docker Development Override
docker-compose.override.yml

# Development & Coordination Tools (PRODUCTION EXCLUDES ALL)
.swarm/
.hive-mind/
memory/
coordination/
.claude/
.mcp.json
claude-flow*
hive-mind-prompt-*.txt
backups/
*.backup
*.bak

# =============================================================================
# MAIN BRANCH INCLUSION STRATEGY
# =============================================================================
# ✅ Essential source code (src/, backend/, frontend/)
# ✅ Core configs (package.json, tsconfig.json, docker-compose.yml)
# ✅ Documentation (README.md, docs/)
# ✅ Production scripts only
# ✅ Shared team settings (.vscode/settings.json)
# ❌ ALL development tools and coordination
# ❌ ALL test artifacts and coverage
# ❌ ALL personal configs and experimental files
# ❌ ALL build outputs (handled by CI/CD)
# MediaNest Backend - Security-Focused .dockerignore
# Prevents sensitive files from entering Docker build context

# Development and build artifacts (security-sensitive)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn
.pnp
.pnp.js

# TypeScript build info (contains source paths)
*.tsbuildinfo
.tsbuildinfo

# Testing and coverage (may contain sensitive data)
coverage/
.coverage
.nyc_output
test-results/
*.spec.ts
*.test.ts
tests/
__tests__/
test-output.log
tests/e2e/test-results/
tests/e2e/reports/
playwright-report/

# Environment files (CRITICAL - contains secrets)
.env*
!.env.example
*.env
.env.local
.env.production
.env.staging
.env.development
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files (may contain sensitive info)
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Logs and temporary files
logs/
*.log
pids
*.pid
*.seed
*.pid.lock
.tmp/
tmp/

# Documentation and non-production files
README.md
*.md
!package.json
docs/
CHANGELOG*
LICENSE*

# Source control
.git/
.gitignore
.gitattributes

# Security and audit files (sensitive information)
audit/
security/
*.pem
*.key
*.crt
*.p12
*.jks

# Database files (if any)
*.sqlite
*.sqlite3
*.db

# Backup files
*.backup
*.bak
*~

# OS generated files
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Docker files (prevent recursive inclusion)
Dockerfile*
docker-compose*
.dockerignore

# CI/CD files (may contain sensitive configurations)
.github/
.gitlab-ci.yml
.travis.yml
.circleci/
azure-pipelines.yml

# Development tools configuration
.eslintrc*
.prettierrc*
.editorconfig
jest.config*
vitest.config*
tsconfig*.json
!tsconfig.json

# Production scripts (security-sensitive)
scripts/
deploy/
infrastructure/

# Monitoring and metrics (may contain sensitive endpoints)
monitoring/
metrics/

# Local development files
.local/
local/
dev/

# Package manager files (build will regenerate clean versions)
jspm_packages/
.node_repl_history
*.tgz
.yarn-integrity

# Runtime directories (will be created in container)
uploads/
downloads/
temp/
cache/
dist/
build/

# Prisma development files
prisma/migrations/
!prisma/schema.prisma
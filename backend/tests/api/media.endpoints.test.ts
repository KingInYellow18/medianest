import { describe, it, expect } from 'vitest';

describe.skip('Media Endpoints - Critical Path', () => {
  it('should implement media request integration with Overseerr', () => {
    expect(true).toBe(true);
  });
});

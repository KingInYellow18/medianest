# =============================================================================
# .gitignore for main branch (Production-Ready Branch)
# Optimized for: Clean production deployments, minimal but essential files
# =============================================================================

# Dependencies (Always exclude)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build Outputs (Exclude - CI/CD rebuilds)
build/
dist/
out/
.next/
coverage/
.nyc_output
*.tsbuildinfo
shared/**/*.js
shared/**/*.js.map
shared/**/*.d.ts
shared/**/*.d.ts.map
!shared/tsconfig.json

# Environment & Secrets (Critical exclusion)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod
.env.test
.env.staging
secrets/
!secrets/.gitkeep
.secrets/
docker-secrets/
*.pem
*.key
*.crt
*.csr
certs/

# Development & Testing (Exclude from production)
test-results/
junit.xml
results.json
visual-regression-diff/
screenshot-diff/
playwright-report/
storybook-static/
.stryker-tmp/

# Personal IDE Settings (Keep shared configs)
.vscode/launch.json
.vscode/extensions.json
.idea/workspace.xml
.idea/tasks.xml
*.swp
*.swo
*~

# OS Files
.DS_Store
Thumbs.db
.Trashes
*.log

# Runtime Data
logs/
postgres_data/
redis_data/
*.db-journal
*.db-wal
*.sqlite-journal
*.sqlite-wal

# Media & Uploads
uploads/
youtube/
downloads/

# Cache & Temp
.cache/
.parcel-cache/
tmp/
temp/

# Docker Runtime
docker-compose.override.yml

# =============================================================================
# CLAUDE FLOW BRANCH: PERSONAL DEVELOPMENT (Include all tooling & experimental)
# =============================================================================
# For claude-flow branch: INCLUDE development tools and coordination files
# This branch is for AI-assisted development with full tooling access

# Claude Flow coordination files (INCLUDE on claude-flow branch)
# NOTE: These are EXCLUDED on main/production but INCLUDED here
# .claude/ (keep for development)
# .mcp.json (keep for MCP integration) 
# claude-flow.config.json (keep for configuration)
# .swarm/ (keep for swarm coordination)
# .hive-mind/ (keep for collective intelligence)
# memory/ (keep for persistent memory)
# coordination/ (keep for agent coordination)

# Still exclude runtime data and large files
*.db
*.sqlite
*.db-journal
*.db-wal
*.sqlite-journal  
*.sqlite-wal

# Large binaries and cache (exclude even on claude-flow)
node_modules/
build/
dist/
.cache/
.parcel-cache/

# Database files (exclude runtime)
*.db
*.sqlite

# Development artifacts
backups/
*.backup
*.bak

# =============================================================================
# MAIN BRANCH INCLUSION STRATEGY
# =============================================================================
# ✅ Essential source code only
# ✅ Core configuration files (package.json, tsconfig.json)
# ✅ Dockerfile and docker-compose.yml (production)
# ✅ Essential documentation (README.md, core docs)
# ✅ Production scripts only
# ✅ Team IDE settings (.vscode/settings.json for consistency)
# ❌ Development tools and coordination
# ❌ Test artifacts and coverage reports
# ❌ Personal development files
# ❌ Build outputs (handled by CI/CD)
# ❌ All sensitive data and secrets
# =============================================================================
# .gitignore for main branch (Production-Ready Branch)
# Optimized for: Clean production deployments, minimal but essential files
# =============================================================================

# Dependencies (Always exclude)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build Outputs (Exclude - CI/CD rebuilds)
build/
dist/
out/
.next/
coverage/
.nyc_output
*.tsbuildinfo
shared/**/*.js
shared/**/*.js.map
shared/**/*.d.ts
shared/**/*.d.ts.map
!shared/tsconfig.json

# Environment & Secrets (Critical exclusion)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod
.env.test
.env.staging
secrets/
!secrets/.gitkeep
.secrets/
docker-secrets/
*.pem
*.key
*.crt
*.csr
certs/

# Development & Testing (INCLUDE test artifacts on test branch)
# test-results/ (keep for analysis)
# junit.xml (keep for CI analysis)
# results.json (keep for test data)
# visual-regression-diff/ (keep for visual testing)
# screenshot-diff/ (keep for visual validation)
# playwright-report/ (keep for test reports)
# storybook-static/ (keep for component testing)
# .stryker-tmp/ (exclude - temporary mutation testing)
.stryker-tmp/

# Coverage data (exclude volatile, keep reports)
coverage/
.nyc_output

# Personal IDE Settings (Keep shared configs)
.vscode/launch.json
.vscode/extensions.json
.idea/workspace.xml
.idea/tasks.xml
*.swp
*.swo
*~

# OS Files
.DS_Store
Thumbs.db
.Trashes
*.log

# Runtime Data
logs/
postgres_data/
redis_data/
*.db-journal
*.db-wal
*.sqlite-journal
*.sqlite-wal

# Media & Uploads
uploads/
youtube/
downloads/

# Cache & Temp
.cache/
.parcel-cache/
tmp/
temp/

# Docker Runtime
docker-compose.override.yml

# =============================================================================
# TEST BRANCH: INCLUDE TEST ARTIFACTS & VALIDATION DATA
# =============================================================================
# Testing and QA coordination (include for test analysis)
# .claude/ (include QA settings)
# .swarm/ (include test coordination)
# test-results/ (include for analysis)
# playwright-report/ (include for debugging)
# visual-regression-diff/ (include for validation)

# Still exclude personal and production secrets
.claude/settings.local.json
claude-flow
claude-flow.bat
claude-flow.ps1
hive-mind-prompt-*.txt

# Include test memory and coordination for analysis
# memory/ (keep test run data)
# coordination/ (keep test orchestration)

# Database files (exclude runtime)
*.db
*.sqlite

# Development artifacts
backups/
*.backup
*.bak

# =============================================================================
# MAIN BRANCH INCLUSION STRATEGY
# =============================================================================
# ✅ Essential source code only
# ✅ Core configuration files (package.json, tsconfig.json)
# ✅ Dockerfile and docker-compose.yml (production)
# ✅ Essential documentation (README.md, core docs)
# ✅ Production scripts only
# ✅ Team IDE settings (.vscode/settings.json for consistency)
# ❌ Development tools and coordination
# ❌ Test artifacts and coverage reports
# ❌ Personal development files
# ❌ Build outputs (handled by CI/CD)
# ❌ All sensitive data and secrets